import React from 'react'


const Box = ({ key, on }) => {
    const styles = {
        backgroundColor: on ? "#222222" : "none"
    }

  return (
    <div style={styles}>
      
    </div>
  )
}

export default Box
